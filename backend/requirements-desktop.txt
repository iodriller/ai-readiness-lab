# Desktop packaging deps — only needed to build/run the native app, not for the
# API test suite. Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-desktop.txt
-r requirements.txt

pywebview==5.3.2
pyinstaller==6.11.1
